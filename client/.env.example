# Client build-time configuration

# Public site origin used in index.html SEO/Open Graph tags (canonical, og:url, og:image).
# Set this to your deployed site's origin. Also update client/public/robots.txt and
# client/public/sitemap.xml, which are served as static files and are not templated.
VITE_PUBLIC_SITE_URL=https://your-domain.example
